# BUILD-USING:        docker build -t data_eval .
# RUN-USING:          docker run --name DATA_EVAL data_eval
FROM          busybox
VOLUME        ["/var/docker/log", "/var/docker/mongod"]
CMD           ["/bin/true"]
